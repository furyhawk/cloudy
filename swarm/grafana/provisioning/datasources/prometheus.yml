# Provisioning config for the Prometheus datasource in Grafana.
# Both services are on the traefik-public overlay network,
# so 'prometheus' resolves via Swarm DNS.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus_prometheus:9090
    isDefault: true
    editable: false
