apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: searxng-claim0
  name: searxng-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
